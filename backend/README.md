# Remote Control Hub

A lightweight remote control and operations platform for managing connected Python-based clients through a web UI.

It combines an interactive shell, remote file operations, script execution, background jobs, artifact collection, agent building, and session-based web access control in a single project.

> **Important**
> Use this project only in environments you own or are explicitly authorized to manage. The repository includes powerful remote execution and persistence-related utilities and should be treated as an administrative/automation tool for controlled use.

---

## Table of Contents

- [Overview](#overview)
- [Core Capabilities](#core-capabilities)
- [Architecture](#architecture)
- [Protocol and Execution Model](#protocol-and-execution-model)
- [Repository Layout](#repository-layout)
- [Web UI Modules](#web-ui-modules)
- [Remote Operations](#remote-operations)
- [Jobs and Scripts](#jobs-and-scripts)
- [Agent Build Modes](#agent-build-modes)
- [Authentication](#authentication)
- [Runtime Data](#runtime-data)
- [Getting Started](#getting-started)
- [Development Notes](#development-notes)
- [Roadmap / TODO](#roadmap--todo)

---

## Overview

Remote Control Hub is a server/client project for operating remote endpoints from a browser-based control panel.

At a high level, it provides:

- A **web console** for shell-like command execution and live output streaming
- A **server CLI** path that can use the same core session/command infrastructure without the browser
- A **device list** with online/offline status
- A **remote file browser** with common file operations
- **Artifacts** for previews and downloads
- **Scripts** for one-shot actions organized by directory
- **Background jobs** for long-running or repeated monitoring tasks
- **Process/app inspection** and kill operations
- **Command history** and execution history views
- **Agent builders** for multiple delivery modes
- A simple **session-based auth layer** plus static API token support

The current implementation is intentionally lightweight and file-based. It does not require a database and stores runtime state under `runtime/`.

The project supports both **server CLI** and **server web UI** entrypoints:

- `ratserver.py` starts the socket server and interactive CLI loop
- `run_web.py` starts the same server core plus the Flask web app

A few implementation-level characteristics are important when reading the rest of this README:

- **Command/control messages** use a custom socket protocol
- **File payload transfer** uses HTTP rather than the command socket
- **Heartbeat/RTT** are first-class connection metrics and are exposed to the UI and server built-ins
- **Watchdog support** is split into a remote control watchdog and a local self-healing watchdog
- The project supports both lightweight **shell-like commands** and structured **`acmd` commands**

---

## Core Capabilities

### 1. Device management

- Lists active client sessions in the left sidebar
- Displays connection metadata such as hostname, IP, platform, integrity, RTT, working directory, and client ID
- Supports recent-device caching so previously seen hosts remain visible as offline entries after refresh
- Uses hostname-based merging for the recent-device cache
- Tracks handshake-reported client metadata such as Python version, process ID, launch command, execution mode, transfer mode, build version, and watchdog flags

### 2. Interactive command execution

- Sends commands from the web UI to the selected client
- Streams command output back to the UI
- Supports command candidates / autocomplete sourced from both server-side built-ins and client manifests
- Includes task cancellation for active web-submitted commands
- Supports both **one-shot return** commands and **streaming** commands
- Supports ordinary **shell-like commands** and structured **`acmd`** execution
- Supports per-command execution context with timeout and cancel state

### 3. Remote file browser

- Browse remote directories
- Create folders
- Rename files/folders
- Delete single or multiple entries
- Download files
- Download multiple paths as a zip archive
- Copy / cut / paste remote paths
- Preview and save remote file content
- Support hidden-file visibility toggle
- Support directory pagination
- Manage pinned quick-jump paths
- Provide breadcrumb navigation in the UI

### 4. Artifact center

- Collects files uploaded from clients or generated by jobs/commands
- Browse artifacts by type and hostname
- Download raw files
- Preview supported artifact content
- Edit artifact content server-side
- Clear artifacts by category
- Preserve file/artifact references so command history and job views can trace related files later

### 5. Process and app inspection

- Enumerate running processes
- View process detail
- Kill processes
- Enumerate running apps separately from raw process views
- Kill apps from the UI

### 6. Script library

- Organizes scripts by directory under `server/resources/scripts/`
- Provides a Script Library dialog with folder navigation and per-script actions
- Supports script metadata via `SCRIPT_METADATA`
- Supports structured script execution via `run_script` while keeping legacy command-style execution paths intact
- Supports script editing, upload, creation, and deletion from the UI
- Includes platform-specific helper scripts, including some basic persistence / privilege-related administrative helpers in the built-in library

### 7. Background jobs

- Lists server-managed job modules from `server/resources/jobs/`
- Supports metadata-driven job catalog presentation
- Supports parameterized job start requests
- Supports job start/stop lifecycle from the web UI
- Supports job result reporting back to the server
- Preserves job-related uploaded files/artifacts for later inspection and download

### 8. Agent generation and update workflow

- Build downloadable client artifacts from the web panel
- Includes multiple build modes such as:
  - **PyInstaller**
  - **Bundle**
  - **Go (Simple)**
  - **Go (Loader)**
- Injects build/version metadata into generated outputs
- Exposes build and download APIs
- Supports an `update` workflow on the Python client that can request a fresh **bundle**, download it automatically, extract it, and launch the new client bundle

### 9. Authentication and access control

- Session-based web login with a fixed admin username/password
- Static token support for API access from tools such as Postman or `requests`
- Route-level anonymous access support via annotation/guard pattern in selected endpoints

---

## Architecture

The project is split into four main parts:

### `server/`
The central web/API and control orchestration layer.

Responsibilities include:
- session and connection registry
- command dispatch
- artifact persistence
- history persistence
- remote file operations orchestration
- script/job catalog and execution requests
- agent build pipeline
- recent-device cache
- web auth
- heartbeat dispatch and RTT tracking
- server built-ins such as `history`, `rtt`, `alias`, `exec`, `run_script`, and watchdog control forwarding

### `client/`
The Python client runtime that connects back to the server and executes commands/jobs/scripts.

Responsibilities include:
- transport and session initialization
- client handshake and manifest reporting
- command execution
- background job execution
- upload and HTTP transfer helpers
- platform-specific command support
- watchdog behavior
- heartbeat acknowledgement and connection metadata refresh

### `core/`
Shared protocol and utility code used by both server and client.

Responsibilities include:
- protocol helpers
- socket framing / message type definitions
- logging utilities
- shared helpers

### `static/`
The browser UI.

Responsibilities include:
- connection list UI
- interactive shell
- file browser
- artifact preview/editing
- history dialogs
- process dialogs
- script/job management dialogs
- login page and auth bootstrap
- terminal JSON table/flat rendering and autocomplete UX

---

## Protocol and Execution Model

### Socket protocol vs HTTP transfer

The project intentionally separates **command/control transport** from **file payload transfer**:

- **Socket protocol** is used for command/control/result messages
- **HTTP protocol** is used for file upload/download and artifact transfer

The socket layer uses JSON messages over a length-prefixed framing protocol (`RATSocket`). Current message families include:

- `command`
- `acmd`
- `script`
- `result`
- `cancel` / `cancel_ack`
- `heartbeat` / `heartbeat_ack`

This split keeps the command channel lightweight while letting file flows use regular HTTP semantics.

Typical mixed flows include:

- a socket command triggering a **screenshot** that is then uploaded as an HTTP artifact
- remote file browser actions using socket commands to orchestrate work, while the actual file bytes move over HTTP
- background jobs uploading generated files while still reporting job state over the normal control channel

### Client handshake

After the TCP connection is established, the client sends an initial `info` handshake payload.

That handshake currently reports runtime details such as:

- client ID
- platform / OS version / hostname
- integrity level
- current working directory
- command manifest
- system paths
- Python version
- process ID / process name / username
- launch command
- current HTTP transfer mode
- current Python execution mode
- remote/local watchdog enabled flags
- client build version

This allows the server UI/CLI to immediately display execution characteristics instead of treating every client as a generic endpoint.

### Heartbeat and RTT

The server periodically sends a `heartbeat` message to each session. The client responds with `heartbeat_ack`, echoing the heartbeat ID and server timestamp while also attaching client-side timing/context fields.

The server uses that round-trip acknowledgement to maintain:

- `last_seen_at`
- `last_heartbeat_sent_at`
- `last_heartbeat_ack_at`
- `last_rtt_ms`

RTT is therefore not a guessed UI field; it is computed from the heartbeat exchange and exposed both in the UI and through the server-side `rtt` built-in command.

### Watchdog model

Watchdog behavior is split into two different responsibilities.

#### Remote HTTP watchdog

The **remote watchdog** is for **out-of-band remote control**.

It runs in the shared watchdog worker and polls a dedicated HTTP endpoint:

- server side queues control actions per client
- client watchdog polls independently of the main socket command stream
- currently supported remote control actions are `kill`, `spawn`, and `reset`

This means the server can still deliver watchdog control commands even when the normal interactive command channel is blocked, busy, or no longer trusted to be the only control path.

The web UI exposes these actions, and the server also provides built-ins such as:

- `force_kill`
- `force_reset`
- `force_spawn`

#### Local watchdog

The **local watchdog** is for **local self-healing / parent liveness monitoring**.

The main client process periodically feeds heartbeat state to a local heartbeat file, and the watchdog worker monitors that file. If the local heartbeat is stale beyond the configured timeout, the watchdog can restart the parent client process.

In short:

- **remote watchdog** = independent remote control channel (`kill` / `spawn` / `reset`)
- **local watchdog** = local crash/hang recovery for the client process itself

A `watchdog` client command also returns a JSON status payload describing the runtime watchdog state.

### Shell-like commands vs `acmd`

The project supports **two command styles**.

#### 1. Shell-like commands

Shell-like commands are lightweight and intentionally simple:

- command name is the first token
- the remaining text is treated as a single raw argument string
- if the command name maps to an exported client method, that method is invoked
- otherwise execution falls back to the generic `shell(...)` handler

This is the familiar path for commands such as:

- `cd ...`
- `spawn ...`
- `read ...`
- `pyexec_collect ...`
- `pyexec_stream ...`
- many file/process/platform helpers

This path is reflection-driven: exported client command methods are discovered from the command object and surfaced through the client manifest.

#### 2. Structured `acmd`

`acmd` is the structured command path:

```text
acmd <command> [--key value] [--key=value] [--flag] [-- positional...]
```

This path is backed by an explicit **command registry** and option specification layer rather than pure reflection.

It is useful when a command needs:

- clearer argument schemas
- typed options
- validation
- positional + named option mixing
- better manifest/help generation

So the project currently supports both:

- a **reflection-style command system** for ordinary exported methods
- a **command registry** system for structured `acmd` commands

### Structured payloads inside shell-like commands

A few web-driven operations still use shell-like commands as the transport surface, but pass structured data encoded as a JSON/base64 payload (for example `__json__:<base64url-json>` style arguments).

This is used in places where the outer command is still convenient as a normal command string, but the payload is naturally structured, such as:

- remote file browser operations
- web-triggered file uploads/downloads
- script/job run requests that need parameter objects

That lets the project reuse the shell-like submission UX without losing structured payload expressiveness.

### Command context, timeout, and cancellation

Each command execution gets a command context that can carry:

- timeout
- cancel event
- cleanup hooks
- cancel policy metadata

This means timeout/cancel is tracked per execution rather than globally.

Important nuance: **cancellation is capability-dependent, not universal**.

Examples:

- normal streaming subprocess commands can be cancelled
- Python execution in `inproc` mode cannot be force-killed like a subprocess
- HTTP transfer in `legacy` mode does not support cancellation
- HTTP transfer in `cancelable` mode does support timeout/cancel-aware streaming

So the accurate mental model is **best-effort cancellation with explicit per-command/per-strategy policy**, not “everything is always interruptible”.

### Python execution strategy

Python code execution supports two runtime strategies:

- **`inproc`** — executes inside the current interpreter with `exec()`
- **`subprocess_pipe`** — starts a child Python process and communicates through stdin/stdout pipes

Trade-offs:

- `inproc` is lightweight and direct, but cancellation/isolation are limited
- `subprocess_pipe` is heavier, but gives better cancellation/timeout/process control behavior

The current runtime configuration selects the active default mode (`PYTHON_EXECUTION_MODE`), and the client handshake reports that mode back to the server.

### Python collect vs stream, and script execution semantics

The project distinguishes **one-shot collected Python execution** from **streaming Python execution**.

Current command-level behavior:

- `pyexec_collect` = execute Python code and return collected output
- `pyexec_stream` = execute Python code with true streaming output
- `script` / `exec <script>` / `run_script` currently execute through the script path and **do not provide true streaming output**

So the important limitation is:

- **streaming Python exec is currently supported through command execution (`pyexec_stream`)**
- **server-side script execution currently behaves as collected output rather than true streaming**

This is especially important when choosing between quick command execution and reusable script-library execution.

### HTTP transfer modes

HTTP transfer currently supports two strategies:

#### `legacy`

- keeps the simpler `requests files=...` upload / `iter_content()` download behavior
- is compatible with the older implementation path
- does **not** support cancellation during upload/download

#### `cancelable`

- uses an interruptible streaming implementation
- supports timeout-aware and cancellation-aware upload/download
- integrates with command context and cleanup handlers

The active transfer mode is reported by the client handshake and affects what the server/UI can truthfully claim about cancel support.

---

## Repository Layout

```text
.
├── client/                 # Python client runtime
├── core/                   # Shared utilities and protocol code
├── server/                 # Web server, APIs, orchestration, runtime services
├── static/                 # Front-end assets (Vue + Element Plus style app)
├── rchclient.py            # Main Python client entry
├── ratserver.py            # Main server entry
└── run_web.py              # Web entrypoint
```

### Key server areas

```text
server/application/
├── agent/                  # Agent builder pipeline
├── artifact/               # Artifact storage / preview / editing
├── command/                # Command execution orchestration
├── connection/             # Connection service + recent device store
├── execution/              # Execution-related abstractions
├── history/                # Command/execution history persistence and views
├── jobs/                   # Background job catalog and runtime APIs
├── pinned_paths/           # Remote file browser pinned paths
├── scripts/                # Script catalog and script execution APIs
├── tasks/                  # Web task submission / cancellation
└── web/                    # Web-facing facades
```

### Key web routes

The current route set includes modules for:

- `agent.py` — agent build/download/platform/loader-report
- `artifacts.py` — artifact list/download/raw/preview/edit/clear/upload
- `background_jobs.py` — jobs catalog/start/stop/download/upload/save/delete/report
- `command_history.py` — history and execution-history endpoints
- `pinned_paths.py` — file browser quick-jump pinning
- `remote_files.py` — remote file operations and preview/edit
- `scripts.py` — script catalog/edit/upload/delete/run
- `system_inspection.py` — processes, apps, system paths

### Built-in script library

The current repository already includes scripts across categories such as:

- `common/gather/*`
- `common/misc/*`
- `mac/gather/*`
- `mac/persist/*`
- `mac/sudo/*`
- `win/cred/*`
- `win/gather/*`
- `win/misc/*`
- `win/ntdll/*`
- `win/persist/*`
- `win/steal_token/*`
- `win/uac/*`
- `win/ui/*`

### Built-in jobs

The current repository already includes jobs such as:

- `app_launch_monitor.py`
- `app_screenshot_monitor.py`
- `clipboard_monitor.py`
- `example_monitor.py`
- `scheduled_screenshot.py`

---

## Web UI Modules

The front end is organized into modular JS files under `static/js/modules/`.

### Main UI areas

- **Connection panel** — device list, current device metadata, recent/offline visibility
- **Terminal panel** — command input, run/cancel, live output, autocomplete
- **Remote Files dialog** — file browser, preview, clipboard operations, quick jumps
- **Artifacts dialog** — artifact listing, preview, content editing
- **History dialog** — quick command history and execution history
- **Processes dialog** — process/app listing and detail views
- **Jobs dialog** — background job module catalog and active jobs
- **Scripts dialog** — script library, metadata-driven execution, edit/upload/delete
- **Agent Builder dialog** — generate agent outputs with different build modes
- **Login page** — session login UI with loading state and server-side auth

### Terminal JSON rendering

The terminal is not limited to raw text output.

When a command output group contains a complete JSON payload, the UI can automatically parse it and render it as:

- a **table** when the payload is an array of objects with a shared shape
- a **flat key/value view** when the payload is a plain object
- a **raw JSON viewer** when table/flat rendering is not a good fit

This is especially useful for commands like `watchdog`, process/file metadata views, and other structured results.

### Autocomplete / candidates

Autocomplete currently merges multiple candidate sources, including:

- server built-in commands
- common ops command suggestions from the front end
- client command manifest entries
- structured `acmd` manifest entries
- script library execution entries such as `exec <script>`
- alias names
- quick-jump entries
- recent command history
- quick-history replay shortcuts such as `!<index>`

In practice this means the command box can surface:

- **server commands**
- **common ops commands**
- **client commands**
- **exec script list entries**
- **recent commands**

### Front-end module list

Current JS modules include:

- `agent.js`
- `artifacts.js`
- `auth.js`
- `candidates.js`
- `commands.js`
- `connection.js`
- `files.js`
- `history.js`
- `jobs.js`
- `preview.js`
- `process.js`
- `scripts.js`
- `sse.js`
- `task.js`
- `terminal.js`
- `utils.js`

---

## Remote Operations

### Command execution

Commands are submitted from the web UI to the selected client. The server tracks active web tasks and streams responses via SSE.

The same backend command pipeline is also usable from the server CLI, so the project is not web-only.

The current command surface includes:

- shell-like commands
- `acmd` structured commands
- server built-ins
- alias expansion
- history replay shortcuts
- script execution and job control flows

### File transfer

The project supports both direct command execution and HTTP-assisted file transfer flows.

Capabilities include:

- web-to-client upload
- client-to-server upload
- zip packaging for batch download
- preview/edit workflows for server-side stored artifacts
- remote browser download/upload helpers that preserve command/job/history association metadata

A useful way to think about the design is:

- **command routing** happens over the socket protocol
- **file bytes** move over HTTP

That split is reused in many places. For example, commands such as screenshot capture, remote-file downloads, and job-generated outputs can all end up as uploaded/downloadable artifact files.

### File browser UX details

The remote file browser supports more than simple listing:

- pin / quick-jump path management
- breadcrumb navigation
- copy / cut / paste of remote paths
- hidden-file toggle
- pagination of directory listings
- preview/edit/save workflows
- single-file download and multi-path zip download

### Process control

From the web UI you can:

- list processes
- inspect process details
- kill individual processes
- list and kill app-style entries

### Connection control

The UI includes high-level control actions such as:

- force kill
- force reset
- force spawn
- disconnect

The `force_*` controls are forwarded through the remote watchdog HTTP control path rather than being simple shell aliases.

---

## Jobs and Scripts

### Background jobs

Background jobs are long-running tasks managed by the server/client job framework.

Current job system highlights:

- job catalog APIs
- metadata-driven parameter forms
- start / stop controls
- report ingestion
- support for platform tags and parameter schemas
- file/artifact association so job outputs remain queryable later

Typical use cases:

- monitoring application launches
- clipboard monitoring
- screenshot or scheduled capture flows

### Scripts

Scripts are one-shot utilities stored under `server/resources/scripts/`.

Current script system highlights:

- directory-based organization
- metadata via `SCRIPT_METADATA`
- script library UI with folder navigation
- edit/upload/delete support
- structured execution via `/api/connections/<client_id>/scripts/run`
- legacy command-style execution through `exec <script>`
- parameter normalization before execution when metadata declares params

This makes scripts well-suited for:

- small data collection tasks
- platform-specific admin actions
- quick UI prompts or persistence helpers
- repeatable one-off actions that do not need a full job lifecycle

### History, quick history, and execution history

The project distinguishes between **quick history** and **execution history**.

#### Quick history

Quick history is designed for fast reuse:

- de-duplicated by command text
- keeps the latest entry per command
- supports pin/unpin
- supports moving pinned items up/down
- is optimized for reuse from the terminal

Replay helpers include:

- `history run <index>`
- `!<index>` as shorthand syntax

#### Execution history

Execution history preserves the full run record:

- no de-duplication
- full command status
- start/end working directory details
- output records
- duration and completion state
- associated file/artifact references

This makes execution history the audit/trace view, while quick history is the reuse/favorites view.

### History/job file association

History and background job records can retain related file references, so later inspection can answer questions such as:

- what files were produced by this command?
- what screenshot/archive/log belongs to this background job?
- is the original artifact still downloadable/previewable?

That linkage is one of the reasons the server records upload metadata like source type, command ID, and related path.

### Alias support

The server supports persistent aliases with simple placeholder substitution.

Current characteristics:

- aliases are stored persistently in JSON
- supports platform groups: `common`, `win`, and `mac`
- effective alias lookup applies the relevant platform overlay
- supports simple positional placeholder parameters such as `<path>` / `<name>`

Examples:

```text
alias ll = read ls -la
alias --platform win desk = cd <path>
unalias ll
```

Important nuance: alias placeholders are intentionally simple and positional. This is not a full shell macro engine.

---

## Agent Build Modes

The project exposes agent-building features from the web UI.

### PyInstaller

- Builds a Python client executable for the current server platform
- Injects build metadata
- Good for environments where Python source bundling is not desired

### Bundle

- Packages `client/`, `core/`, and `rchclient.py` into a zip archive
- Injects build version into the bundle configuration
- Designed for source-based delivery/update flows
- Used by the Python-client `update` workflow to fetch and launch a newer bundle automatically

### Go (Simple)

`Go (Simple)` is positioned as a **small and lightweight baseline client** for cases where a compact executable is preferred and the full Python/PyInstaller delivery path is too heavy.

Current intent and positioning:

- minimal cross-platform standalone Go client path
- better fit for lightweight execution scenarios
- useful when PyInstaller output is too bulky
- useful when a full source bundle is not convenient to land on the target

Compared with the Python client, the current Go (Simple) path is intentionally basic and should be understood as a minimal feature subset.

It is intended for simple capabilities such as:

- unified heartbeat/message protocol participation
- basic shell execution
- `cd` / `pwd`-style command support
- `getinfo`-style environment reporting
- screenshot/basic artifact upload flows
- basic file transfer/download-oriented operations

It is **not** the feature-complete path today. Advanced Python-client features such as the richer remote file browser workflow, background jobs, script library execution, and watchdog-related functionality should be considered Python-client-first for now.

### Go (Loader)

The Go loader has a different responsibility from the simple Go build.

Current loader responsibilities include:

- collecting local environment basics such as OS/arch/host/IPs
- reporting to the server via HTTP
- detecting Python availability and reporting the result
- resolving a working directory under the user home directory
- requesting a fresh bundle build from the server
- downloading and extracting the bundle
- launching `rchclient.py` via `python3` / `python`
- reporting launch details (paths, PID, build version)
- exiting after handoff

The server side includes a loader report endpoint that currently writes JSON-line style logs under runtime logs.

---

## Authentication

The web app includes a lightweight auth layer designed for simple public deployment scenarios.

### Supported auth methods

- **Session login** for the browser UI
- **Static API token** for external API clients

### Current characteristics

- fixed admin username/password stored in server config / environment variables
- no registration flow
- no database dependency
- session cookie based login for the web app
- API token accepted via auth guard

### Relevant config entries

- `RCH_WEB_SESSION_SECRET`
- `RCH_ADMIN_USERNAME`
- `RCH_ADMIN_PASSWORD`
- `RCH_ADMIN_API_TOKEN`
- `RCH_WEB_AUTH_SESSION_DAYS`
- `RCH_WEB_SESSION_COOKIE_NAME`

---

## Runtime Data

The project stores runtime data under `runtime/` rather than a database.

Examples include:

- artifacts
- command history
- execution history details
- pinned paths / quick jumps
- web files / previews
- agent build outputs
- loader logs
- recent devices cache

### Recent devices cache

A lightweight recent-device cache is maintained at:

```text
runtime/recent_devices.json
```

Current behavior:

- one record per hostname
- online device updates overwrite prior cached state
- offline devices remain visible as recent entries after refresh

---

## Getting Started

## Requirements

Typical development/runtime requirements include:

- Python 3.x
- project Python dependencies for server and client
- a browser for the web UI
- optional Go toolchain if you want to build Go-based outputs

## Run the server

Typical entrypoints in the repo:

- `ratserver.py` — socket server + CLI
- `run_web.py` — socket server + heartbeat runner + web app

## Run the client

Client entrypoint:

- `rchclient.py`

## Access the web UI

Main static pages currently include:

- `static/index.html`
- `static/login.html`

If auth is enabled, sign in with the configured admin credentials before using the control panel.

---

## Development Notes

### Front end

The current UI is a classic static app structure with modular JavaScript and Element Plus style components. It is not yet on a Vite + Vue 3 full build pipeline.

### Metadata-driven extensibility

The project now uses metadata-style declarations in both jobs and scripts:

- `JOB_METADATA`
- `SCRIPT_METADATA`

This enables:
- platform filtering
- parameter prompting
- richer catalog views in the UI

### Storage model

The current architecture intentionally favors:
- runtime JSON/files
- lightweight stores
- no database setup

That keeps iteration fast, but some subsystems still need convergence and cleanup.

### Execution model notes

A few subtle implementation notes are worth calling out explicitly:

- command cancellation support depends on the command/strategy being used
- HTTP transfer cancel support depends on whether transfer mode is `legacy` or `cancelable`
- Python execution semantics differ between `inproc` and `subprocess_pipe`
- true streaming Python execution currently exists at the command level (`pyexec_stream`) rather than the reusable script-library path
- server built-ins, aliases, client manifests, and history shortcuts all participate in the final terminal UX together

---

## Roadmap / TODO

Below is a reorganized roadmap based on current priorities and project direction.

### High-priority architecture and refactor work

- [x] Unify history queries around **hostname** as the primary lookup model
- [x] Add a centralized whitelist/annotation-based policy to decide which commands should be recorded in history
- [ ] Introduce a consistent exception/error model across server and client layers
- [ ] Refactor duplicated fields and behaviors across **history / task / job** models
- [ ] Rebuild the front end with **Vite + Vue 3**

### High-priority fixes

- [ ] Fix background jobs currently depending on HTTP so they can also work correctly in CLI mode
- [ ] Fix stream cancellation for `exec script` mode so the stream actually closes when cancelled
- [ ] Fix alias resolution to match by name instead of argument count, and support reusing the same variable multiple times
- [x] Fix occasional `client is busy` errors during process listing and process kill operations

### Shell, execution, and terminal improvements

- [x] Add **xterm.js + PTY** support
- [ ] Support client-side variables in commands, for example `spawn ${exec_path}`
- [ ] Support inline helper expressions such as `kill ${getpid}`
- [ ] Add reverse shell support
- [ ] Add multi-device batch execution
- [ ] Support writing command output to files, similar to shell-style redirection
- [ ] Add a normal JSON / UI toggle inside the terminal JSON dialog
- [ ] Make `read` support built-in commands

### Jobs and background execution

- [ ] Add detached / process-based jobs
- [ ] Persist jobs instead of keeping job runtime state purely in memory

### Device and connection model

- [x] Distinguish **session ID** and **client/machine ID** more clearly
- [x] Introduce a `machine_id` derived from stable host traits (for example hostname + MAC-related traits) to better correlate repeat connections from the same machine
- [ ] Add hide/remove connection support in the UI

### Process, files, and visibility improvements

- [ ] Add thread-level display to process views
- [ ] Add file search

### Safety, confirmations, and workflow UX

- [ ] Add prompt/confirm workflows for commands, including multi-stage confirmation and danger annotations for risky actions

### Remote visibility and control expansions

- [ ] Add screen view support
- [ ] Add listener management, including creating and switching listener types such as WS / TCP / HTTP
- [ ] Add notifier integrations for online alerts, such as email or SMS
- [ ] Add an external tool manager for companion tooling such as FRP or VNC
- [ ] Add Telegram bot support

---

## Current Status Summary

Today, the project already provides a fairly broad remote operations surface:

- device management
- interactive command execution
- remote file browsing and editing
- artifacts
- scripts
- jobs
- process/app inspection
- history
- agent generation
- basic web auth
- recent-device caching
- heartbeat/RTT visibility
- watchdog-assisted control and recovery

The next major milestones are mostly about **consolidation**:
- cleaner data models
- stronger execution UX
- better session/machine identity separation
- a more modern front-end stack

---

## Limitations / Known Gaps

The following limitations come from the current execution model, transport split, and agent/runtime boundaries. Several of them are structural trade-offs rather than short-term bugs, so they may remain unresolved for a long time.

1. **A single client effectively exposes only one active foreground slot at a time**
   - Foreground command execution, web-submitted tasks, uploads, remote file operations, process inspection, and similar interactive actions all contend for the same active execution slot on one client.
   - As a result, the project is currently optimized for one operator / one active interaction flow per client, not true concurrent multi-operator control on the same session.
   - When another foreground action is already running, later requests will typically fail with a busy-state response rather than being queued and multiplexed.
   - If parallel work against the same target is required, the practical workaround is to launch another connection / agent instance instead of sharing the same one.

2. **Script-style streaming execution does not have fully reliable cancellation semantics**
   - Cancellation is best-effort, not a hard guarantee, for script-oriented streaming paths.
   - The runtime can propagate cancel requests and can terminate some subprocess-backed execution paths, but it cannot reliably unwind work that is already executing inside the current interpreter or already inside user script logic.
   - In other words, the UI/server can request cancellation, but “stop immediately and cleanly at any point” is not something the current script execution model can guarantee.

3. **There is still no single lightweight, cross-platform, feature-complete agent form factor**
   - The build pipeline is currently split across different delivery styles (for example, Python bundle / PyInstaller / Go-related build paths), which reflects a trade-off rather than a solved packaging story.
   - Heavier builds provide broader feature coverage, while lighter paths are better for footprint but do not yet represent a clean “small binary with full feature parity everywhere” solution.
   - A truly lightweight executable agent that is also cross-platform and feature-complete is still missing.

4. **Direct server-local operations are still not a first-class surface**
   - Most operational APIs and UI flows are connection-scoped and target remote clients.
   - The project still lacks an equally complete server-local operation plane for things like direct server shell access, general server-side file management, and similar local administration workflows.
   - In practice, the server mainly acts as coordinator, storage layer, command router, and web/API host rather than a full local administration console.

5. **Command output and message shapes are not yet normalized across the entire system**
   - Different command families currently return different result styles: human-readable text blocks, table-like text, raw JSON text, structured API payloads, SSE chunks, artifact/file messages, and command-specific ad hoc formats.
   - Some commands expose a JSON mode, while others are still primarily text-oriented.
   - This makes downstream parsing, generic UI rendering, and third-party automation more brittle than they should be.

6. **Some features rely on third-party Python packages that are only imported at execution time**
   - A number of commands lazily import optional dependencies only when the relevant command is executed.
   - This keeps startup and baseline deployment simpler, but missing dependencies only fail at runtime, not at boot time.
   - In practice, operators may still hit runtime import failures for dependency-backed features such as process inspection, screenshot/UI automation, networking helpers, platform-specific integrations, image handling, or package inspection.
   - A more explicit dependency bundling / on-demand dependency delivery model may be added later, but it is not solved yet.

7. **There is no strong, stable machine identity layer yet**
   - Several parts of the system still correlate devices using weak identifiers such as hostname, and the recent-device cache is merged primarily on hostname.
   - History, artifacts, and job-related records also carry session/client identifiers that are useful operationally but are not durable enough to behave like a real machine identity.
   - This can cause collisions or weak deduplication when multiple machines share similar names, or when reconnects create fresh session/client identifiers.
   - A persistent machine-level identifier has not been fully introduced yet.

8. **Shell execution is not PTY-backed and does not support real interactive stdin**
   - Shell-like commands are launched as one-shot subprocess executions rather than as a true interactive terminal session with a PTY.
   - Commands that require live stdin interaction, password prompts, full-screen TUI behavior, or step-by-step confirmation flows are therefore not handled well.
   - This affects cases such as interactive privilege elevation, confirmation-driven shell programs, and terminal UI applications.
   - The same limitation also applies to built-in command flows: the current command architecture is fundamentally request/stream/result oriented, not a multi-stage prompt/response conversation model.

---

## License / Usage Note

Before publishing this repository, add the license and usage policy that match your intended deployment model.

For public sharing, it is strongly recommended to include:
- an explicit license
- an authorization-only usage statement
- a note describing the intended admin / automation use cases



---

# Current Feature Supplement (2026-09-04)

> **Additive documentation only.**
>
> Everything above this line is preserved from the original README. This section exists because the original README describes the broad architecture but does not enumerate a large amount of the current command surface, SDK surface, runtime stores, and newer Web UI functionality.
>
> This supplement was generated against the current code baseline after the sidebar-drag / `__init__.py` experiment was rolled back. It intentionally documents what is present in the code without rewriting the historical README above.

## Audit scope

The current source tree exposes substantially more functionality than the original feature summary suggests.

A code-level inventory currently contains:

- **10 Server terminal built-ins**
- **71 user-facing Client commands** carrying command-manifest descriptions
- **17 structured `acmd` command specs**
- **6 Script SDK modules**
- **40 Vue components**
- **158 HTTP route handlers**

The Client command reference below intentionally includes only commands whose manifest metadata uses `suggest=True`, meaning commands intended to appear in normal UI/autocomplete discovery. Internal Web plumbing and helper commands marked `suggest=False` are omitted. Platform-specific commands are only available on supported targets.

---

## Server terminal built-ins

The Server terminal has its own command layer in addition to Client commands and `acmd`.

| Command | Purpose | Important behavior |
| --- | --- | --- |
| `upload <local_file>` | Upload a Server-local file to the selected Client | Uses the existing web/upload execution path rather than embedding file bytes in the command socket |
| `exec <script.py>` | Execute a Server Script Library file on the selected Client | Uses the Script execution plan |
| `run_script <payload>` | Execute a structured Script Library payload | Used by metadata-driven Script UI execution |
| `alias ...` | Manage and resolve command aliases | Supports `resolve`, `set`, `unset`, `list`, `reload`, platform scopes, and JSON/table output |
| `history` | Show quick command history | Supports `history run <index>`, `history clear`, and the `!<index>` shortcut |
| `gopin [name]` | List or jump to a machine-scoped pinned remote path | Resolves the saved path into a real `cd` command while preserving the original `gopin` text in history |
| `rtt` | Show heartbeat RTT / last-seen state | Reads the live connection/session metrics |
| `httpctl stop|restart|start` | Queue an independent HTTP control action for the selected Client | Does not depend on the normal command socket remaining usable |
| `saveout <client-command>` | Run a Client command and save the complete output as an Artifact | Creates a `command_output` Artifact and records source metadata |
| `xt ...` | External Tool CLI facade | Supports list/info/which/direct execution of already-installed external-tool executables |

### `alias`

Alias configuration is persisted in `server/resources/aliases.json`.

Supported scopes:

```text
common
win
mac
linux
```

Examples:

```text
alias resolve
alias list
alias list --platform mac
alias set ll = shell ls -la
alias set --platform win psl = acmd ps --sort cpu
alias unset ll
alias reload
```

Aliases are merged as:

```text
common
+
current platform
```

Platform-specific entries override same-name common aliases for that target.

Alias entries are also inserted into terminal autocomplete candidates.

### `history`, Quick History, and pinned commands

Quick History is not just a projection of raw execution history.

Pinned commands are stored separately per `machine_id` under:

```text
runtime/command_history/pinned_commands/
```

The pinned store keeps:

- command text
- pinned time
- explicit pin order
- a small execution snapshot
- file/artifact references from the seed execution when available

It deliberately does **not** duplicate full execution output records.

Supported UI behavior includes:

- pin / unpin
- stable pinned order
- move pinned command up/down
- execute an old command again
- `history run <index>`
- `!<index>`
- autocomplete integration

The history replay path rewrites the new execution entry to the actual resolved command instead of polluting history with repeated `history run ...` meta commands.

### `gopin` and pinned remote paths

Pinned remote paths are machine-scoped workspace shortcuts stored under:

```text
runtime/pinned_paths/<machine_id>.json
```

Each entry contains:

```text
display_name
path
created_at
updated_at
```

They are available through three surfaces:

1. **Remote Files UI**
   - save current path
   - edit pin name/path
   - remove pin
   - jump to pin

2. **Terminal**
   ```text
   gopin
   gopin <display_name>
   ```

3. **Script SDK**
   ```python
   from client.runtime.sdk import workspace

   workspace.list()
   workspace.as_dict()
   workspace.get("logs")
   workspace.path("logs")
   ```

`gopin` has its own autocomplete provider, so saved display names appear as command candidates.

---

## Client execution commands

The Client command manifest contains the common execution primitives that are easy to miss when only looking at Web dialogs.

### Core execution primitives

```text
cd
shell
spawn
read
pyexec_collect
pyexec_stream
```

Their roles are intentionally different:

| Command | Behavior |
| --- | --- |
| `cd <path>` | Changes the Client process working directory; without an argument, reports current CWD |
| `shell <command>` | Runs a shell command and waits for the final result |
| `spawn <command>` | Starts a background process and returns immediately with the PID |
| `read <command>` | Runs a subprocess while streaming output live to the terminal |
| `pyexec_collect <code>` | Executes Python and returns collected output |
| `pyexec_stream <code>` | Executes Python with streaming output |

Windows additionally exposes:

```text
run <command>
```

which starts a program detached without waiting.

Normal shell syntax is passed through to the target shell, so regular shell redirection can be used where the target shell supports it.

### Python execution modes

Python execution has two runtime strategies:

```text
inproc
subprocess_pipe
```

The active strategy is controlled by:

```text
PYTHON_EXECUTION_MODE
```

and is exposed through Client Runtime Configuration.

`subprocess_pipe` isolates the Python command in a subprocess while preserving collected/streamed output behavior.

### Session lifecycle commands

```text
kill
reset
reexec
spawn_instance
```

- `kill` terminates the current Client session/process.
- `reset` starts a fresh Client process and reconnects.
- `reexec` replaces the current process image.
- `spawn_instance` launches an additional Client instance without terminating the current one.

This is important because multiple Client instances on one `machine_id` are a valid operating model, for example user/admin/system sessions.

### Runtime/session maintenance commands

```text
watchdog
update
clean_releases
set
help
```

- `watchdog` returns the current guard/watchdog runtime state as JSON.
- `update` builds/downloads/extracts/starts the newest bundle.
- `update --keep-old` leaves the current Client alive after the new Client successfully starts.
- `clean_releases` removes obsolete local bundle releases and ZIPs.
- `set` reads/writes Client runtime configuration overrides.
- `help` is generated from the actual exported command manifest.

---

## User-facing Client command reference

The following tables are generated from current `@desc(...)` declarations with `suggest=True`. Commands marked `suggest=False` are implementation-facing helpers and are intentionally not listed as user commands here. Availability still depends on platform.

### Common commands

| Command | Group | What it does |
| --- | --- | --- |
| `download` | `file` | Download a file from the client |
| `unzip` | `file` | Extract a ZIP archive |
| `zip_dir` | `file` | Create a ZIP archive |
| `job_status` | `job` | Show background job status |
| `jobs` | `job` | List available background jobs |
| `jobs_ps` | `job` | List running background jobs |
| `start_job` | `job` | Start a background job |
| `stop_all_jobs` | `job` | Stop all background jobs |
| `stop_job` | `job` | Stop a background job |
| `ifconfig` | `network` | Show network interfaces |
| `ip` | `network` | Show network IP addresses |
| `set` | `runtime` | Show or update client runtime config |
| `clean_releases` | `session` | Clean outdated client bundle release directories and ZIP files |
| `help` | `session` | Show available commands |
| `kill` | `session` | Terminate current session |
| `reexec` | `session` | Restart current process by exec replacement |
| `reset` | `session` | Restart client process and reconnect |
| `spawn_instance` | `session` | Start a new client instance without exiting current process |
| `update` | `session` | Build, download, extract and launch the latest client bundle; use --keep-old to retain current client |
| `watchdog` | `session` | Show watchdog runtime status as JSON |
| `cd` | `shell` | Change working directory |
| `pyexec_collect` | `shell` | Execute Python code and collect output |
| `pyexec_stream` | `shell` | Execute Python code with streaming output |
| `read` | `shell` | Run a command with live output |
| `shell` | `shell` | Run a shell command |
| `spawn` | `shell` | Run a program in background |
| `getpid` | `system` | Get current process ID |
| `getuid` | `system` | Get current user ID/name |
| `is_root` | `system` | Check if current session is root |
| `keyboard_send` | `system` | Simulate keyboard input |
| `pgrep` | `system` | Find processes by name |
| `piplist` | `system` | List installed third-party Python packages |
| `pkill` | `system` | Terminate processes by name |
| `pwd` | `system` | Print working directory |
| `uptime` | `system` | Show system uptime |
| `userenum` | `system` | List system user accounts |

### Windows commands

| Command | Group | What it does |
| --- | --- | --- |
| `getinfo` | `platform` | Get system information |
| `idletime` | `platform` | Get user idle time |
| `screenshot` | `platform` | Capture screenshot |
| `run` | `shell` | Run a program without waiting (detached) |
| `runasadmin` | `system` | Run command as admin |

### macOS commands

| Command | Group | What it does |
| --- | --- | --- |
| `file_shred` | `file` | Securely delete file (overwrite) |
| `getinfo` | `platform` | Show system information |
| `idletime` | `platform` | Show user idle time |
| `screenshot` | `platform` | Capture a screenshot |
| `sudo_run` | `platform` | Run command with sudo |
| `sudo_self` | `platform` | Launch new instance with sudo |
| `webcam_snap` | `platform` | Capture webcam photo |

### iOS commands

| Command | Group | What it does |
| --- | --- | --- |
| `clearconsole` | `console` | Clear console output |
| `keepawake` | `console` | Keep screen awake |
| `killapp` | `console` | Force quit app |
| `openin` | `console` | Open file in other app |
| `openurl` | `console` | Open URL |
| `pickupload` | `console` | Pick photo or file and upload |
| `quicklook` | `console` | QuickLook preview file |
| `getlocation` | `mobile` | Get current location |
| `listcontacts` | `mobile` | List contacts via CNContactStore |
| `readclip` | `mobile` | Read clipboard text |
| `speak` | `mobile` | Speak text |
| `writeclip` | `mobile` | Write clipboard text |
| `getinfo` | `platform` | Get system information |
| `cat` | `shell` | Read text file |
| `echo` | `shell` | Echo text or expand environment variables |
| `ls` | `shell` | List directory contents |
| `md5sum` | `shell` | Compute file MD5 |
| `mkdir` | `shell` | Create directory |
| `printenv` | `shell` | Print environment variables |
| `rm` | `shell` | Remove file |
| `rmdir` | `shell` | Remove directory recursively |
| `touch` | `shell` | Create empty file |
| `whoami` | `shell` | Show current username |

> Linux currently relies primarily on the common command set plus common `acmd` specs.

---

## Structured `acmd` commands

`acmd` is a separate structured command surface with schema-backed argument validation and help.

Current specs:

| Platform | `acmd` name | Description |
| --- | --- | --- |
| Common | `netstat` | Show TCP/UDP network connections |
| Common | `ps` | Show running processes |
| iOS | `alert` | Show alert dialog |
| iOS | `notify` | Send local notification |
| iOS | `find` | Find files by keyword |
| iOS | `tree` | Show directory tree |
| iOS | `head` | Show first N lines of a text file |
| iOS | `tail` | Show last N lines of a text file |
| iOS | `wget` | Download file from URL |
| iOS | `tcp_ping` | Measure TCP connection latency to a target host and port |
| iOS | `ping` | Send ICMP echo requests to a target host to measure network latency |
| macOS | `msgbox` | Show a native macOS dialog |
| macOS | `notify` | Show a native macOS notification |
| macOS | `sqlite_query` | Read-only SQLite query |
| macOS | `image_info` | Show image metadata (size, resolution, color mode, EXIF) |
| macOS | `import_check` | Check Python package/module status |
| macOS | `archive_peek` | List archive contents without extracting |

Common examples:

```text
acmd ps
acmd ps --pid 123
acmd ps --name python
acmd netstat
```

Platform specs add richer argument-aware commands without forcing those semantics into the lightweight shell-like command model.

---

## Client command variables

Ordinary Client command strings support runtime variable interpolation before command dispatch.

Supported namespaces:

```text
${rch:*}
${env:*}
${path:*}
```

### `rch`

Current built-ins include:

```text
${rch:exec_path}
${rch:launch_command}
${rch:pid}
${rch:uid}
${rch:cwd}
${rch:shell}
${rch:hostname}
${rch:command_id}
${rch:client_id}
${rch:machine_id}
${rch:build_ver}
${rch:py_ver}
${rch:platform}
${rch:os_type}
${rch:os_alias}
${rch:os_name}
${rch:os_ver}
${rch:arch}
${rch:manufacturer}
${rch:model}
${rch:timestamp}
${rch:datetime}
```

Examples:

```text
spawn ${rch:exec_path}
shell echo ${rch:uid}@${rch:hostname}
shell mkdir ${rch:timestamp}
kill ${rch:pid}
```

### `env`

Environment variables are read dynamically from the target Client process:

```text
${env:HOME}
${env:TEMP}
${env:APPDATA}
${env:PATH}
```

The environment namespace is not a hard-coded OS list; autocomplete candidates come from the actual Client environment.

### `path`

Current system-path shortcuts:

```text
${path:home}
${path:desktop}
${path:downloads}
${path:temp}
```

The Client includes a `variable_manifest` in its handshake so the Server/Web UI can expose completion/help without duplicating the variable list in Frontend code.

---


## Terminal command blocks and output UX

The terminal UI is built around command/execution blocks rather than a single flat text buffer.

Current block/output behavior includes:

- one logical block per command execution
- streaming chunks appended to the active block
- command start/final status
- duration/status metadata
- semantic output markers such as info/success/warning/error
- block collapse / expand
- resend command from the block menu
- save command output as Artifact
- linked/clickable URL handling
- JSON detection and structured viewer
- JSON table/flat-style inspection
- copy raw JSON
- switch between rendered UI and raw JSON where supported
- command history/execution detail linkage
- artifact/file linkage from command results

The terminal keeps command history, pinned history, aliases, Server built-ins, Client manifest commands, `acmd`, and Client variable completion in the same autocomplete workflow.

The command input closes/reloads completion state after execution so stale candidates do not remain visually selected.


## Watchdog and independent HTTP control

The watchdog implementation has two independent responsibilities.

### Remote HTTP watchdog

When enabled, a separate watchdog worker polls:

```text
GET /api/connections/<client_id>/control
```

for one of:

```text
stop
restart
start
```

The Server can queue those actions with:

```text
httpctl stop
httpctl restart
httpctl start
```

or the corresponding control API.

This path exists specifically so the Server can still request stop/restart/start when the main command socket is blocked, broken, or unusable.

The remote watchdog:

- runs outside the Client foreground command slot
- has its own polling interval
- writes its own watchdog log
- can kill the parent Client
- can restart the parent Client
- can start an additional Client instance

### Local watchdog

The local watchdog uses a heartbeat file written by the running Client.

If the watchdog worker sees the heartbeat go stale past the configured timeout, it can restart the parent Client process.

### Runtime configuration

Relevant keys include:

```text
REMOTE_HTTP_WATCHDOG_ENABLED
REMOTE_HTTP_WATCHDOG_INTERVAL_SECONDS
LOCAL_WATCHDOG_ENABLED
LOCAL_WATCHDOG_HEARTBEAT_INTERVAL_SECONDS
LOCAL_WATCHDOG_TIMEOUT_SECONDS
```

The main Client command:

```text
watchdog
```

returns the current watchdog/guard runtime status.

---

## Runtime Configuration and `set`

Client runtime settings are no longer expected to be edited by modifying bundled source files.

Defaults live in:

```text
client/config/runtime_config.py
```

Overrides are written to an external:

```text
runtime_config.json
```

The exact location is platform-aware and can also be forced with:

```text
RUNTIME_CONFIG_PATH
```

Typical platform locations:

```text
Windows: %APPDATA%/rchclient/runtime_config.json
macOS:   ~/Library/Application Support/rchclient/runtime_config.json
Linux:   ~/.config/rchclient/runtime_config.json
iOS:     ~/Documents/.rchclient/runtime_config.json
```

Current exposed configuration areas include:

- command shell/stream timeout
- HTTP transfer mode
- transfer buffer size
- transfer idle timeout enable/seconds
- preview image compression enable/quality
- Python execution mode
- reconnect interval
- remote HTTP watchdog enable
- local watchdog enable

The terminal supports:

```text
set
set KEY value
set --reset KEY
set --reset-all
set --all
```

The Connection Info **Configuration** tab reads the Client-provided runtime-config metadata dynamically; the Frontend does not hard-code the config catalog.

---

## Machine identity, device state, and connection history

### `machine_id` vs `client_id`

The current connection model distinguishes:

```text
machine_id = stable machine identity
client_id  = one Client process/session identity
```

A single machine may legitimately have multiple simultaneous `client_id` values.

### Stable machine ordering

Recent devices persist a `machine_order` value in:

```text
runtime/recent_devices.json
```

Behavior:

- a new machine is assigned a position the first time it is seen
- reconnecting or receiving a new `client_id` does not move that machine
- existing `machine_order` is preserved
- the JSON can be edited manually to change order
- multiple Clients on the same machine are ordered internally by newest `connected_at`

There is currently **no drag-to-reorder implementation**; the previous experiment was rolled back.

### Restore selected machine after refresh

The Frontend remembers the previously selected `machine_id` for the browser tab.

On reload:

```text
previous machine still exists
    -> select its preferred/current Client

previous machine no longer exists
    -> select nothing
```

It does not silently fall back to the first unrelated device.

### Device Groups

Groups are attached to **machine_id**, not to `client_id`.

Capabilities:

- create group
- rename group
- delete group
- assign a machine from the Sidebar
- filter Sidebar by group
- preserve hidden-device behavior inside the active group
- automatically group every Client belonging to that machine

Runtime file:

```text
runtime/device_groups.json
```

### Machine alias / hide preferences

The current device view supports:

- machine alias
- hide individual Client
- hide entire machine
- show hidden devices

These preferences are returned with the connection/device view and persisted through Server runtime state rather than being limited to a random Client session.

### Machine Connection History

The dedicated Machine Connection History dialog can show historical sessions for the same machine, including:

- Client ID
- build version
- connect/disconnect timestamps
- session duration
- command counts/statuses
- user/process/OS context where available
- linked execution history

---

## Client source revision and update detection

`build_version` and `client_revision` are separate concepts.

```text
build_version    = one build artifact identity
client_revision  = source-content identity
```

The revision covers the actual Client bundle roots:

```text
client/
core/
rchclient.py
```

and excludes configuration/runtime/build noise, notably:

```text
client/config/
__pycache__/
.git/
.idea/
.vscode/
venv/
node_modules/
dist/
build/
*.pyc
*.pyo
*.pyd
.DS_Store
```

This is exclusion-based, so newly added code directories are included automatically.

### Live Server revision

The Server does not rely on a revision value cached only at Server startup.

For an online connection it can compute the current on-disk Server Client-source revision and compare it with the Client handshake revision.

Selecting a Client can request:

```text
GET /api/connections/<client_id>/revision-status
```

which can return:

- current revision
- Server revision
- state
- changed-file information where the Client supplied a file manifest

Offline Clients are not repeatedly revision-checked.

### Terminal warning

An outdated Client is still usable.

The terminal appends one revision warning instead of blocking the session.

For normal bundle Clients the warning can provide:

```text
[ Update ] [ Details ]
```

For `build_version=dev`, the UI does **not** recommend `update`; it tells the operator to restart the Client from the IDE/source environment.

The details dialog can show:

- current revision
- Server revision
- added files
- modified files
- removed files

---

## Safe Client update handoff

The current `update` path is more conservative than simply starting a new PID and killing the old process immediately.

Default flow:

```text
request bundle
download ZIP
safe extract
launch new Client
wait for new Client handshake/ready marker
verify new process remains alive
only then exit old Client
```

If the new Client fails before the handoff is considered healthy:

- the old connection stays alive
- startup stderr/error text is returned when available

Conservative mode:

```text
update --keep-old
```

starts the new Client but keeps the old Client alive as well.

This matters because multiple Client instances on one machine are a supported operational case.

iOS retains its existing update/restart behavior rather than forcing the desktop handoff model onto Pythonista/iOS.

---

## Remote Files: current extended surface

The current Remote Files implementation is significantly larger than the original short list.

### Browse/navigation

- breadcrumb navigation
- direct path input
- pagination
- file search
- hidden-file toggle
- system quick paths
- machine-scoped pinned paths
- Remote Pins management dialog
- remote file/folder picker used by Script/Job metadata forms

### File operations

- create directory
- rename
- delete one
- delete many
- cut/copy/paste
- move
- upload
- drag-to-upload
- multi-select
- multi-path download
- multi-path ZIP creation

### ZIP operations

Client file commands include:

```text
create_zip_paths
peek_zip
read_zip_entry
extract_zip_path
zip_dir
unzip
```

The Web UI includes a dedicated ZIP Peek dialog and **Extract Here** flow.

ZIP entry reading supports inspecting text content without first extracting the entire archive.

### Preview/edit

- text preview
- line-numbered editor
- save edited remote file
- image preview
- image compression option before preview upload
- image resolution / metadata / EXIF display where supported
- preview cache

### Occupied-file process inspection

Remote file flows can inspect which process occupies a file and link into process information.

---

## Transfer subsystem

File transfer is no longer just “HTTP upload/download helpers”.

Current transfer capabilities include:

- Client TransferManager
- Server TransferService
- transfer IDs
- running state
- progress bytes/percentage
- cancel support on the cancelable path
- recent transfers
- Transfer Center drawer
- transfer SSE events
- notification preference integration

### Transfer modes

```text
legacy
cancelable
```

`legacy` is intentionally retained as a simple compatibility path.

#### Legacy upload

Legacy upload uses `requests` multipart `files=...`, which buffers the multipart body in memory.

Current protection:

```text
Legacy upload > 1 GB -> rejected
```

with guidance to use cancelable mode for large files.

#### Legacy download

Legacy download uses:

```text
stream=True
iter_content(...)
```

so it does not use the same 1 GB upload restriction.

### Runtime knobs

```text
HTTP_TRANSFER_MODE
HTTP_TRANSFER_BUFFER_SIZE
HTTP_TRANSFER_IDLE_TIMEOUT_ENABLED
HTTP_TRANSFER_IDLE_TIMEOUT_SECONDS
```

The default idle timeout is a no-progress timeout, not a total transfer-duration limit.

---

## Artifact categories and Shared Files

The formal Artifact types are:

```text
files
previews
shared_files
command_output
```

### `files`

Normal Client-produced/downloaded file results.

### `previews`

Temporary preview artifacts/caches.

### `shared_files`

`shared_files` is a global/shared Artifact area rather than a per-machine result category.

The Artifact UI supports:

- Shared Files tab
- create a new shared text file
- upload shared files
- preview
- edit text content
- rename
- delete
- send a Shared File to the currently selected Client

The Server explicitly allows “send Artifact to Client” only for `shared_files`.

The Script SDK can also use Shared Files as a lightweight workspace:

```python
from client.runtime.sdk import artifact

saved = artifact.save("/tmp/tool.zip", type="shared_files")
items = artifact.list(type="shared_files")
meta = artifact.get(saved["artifact_id"], type="shared_files")
local_path = artifact.download(saved["artifact_id"], type="shared_files")
```

### `command_output`

Command output can be stored as a first-class Artifact.

The terminal/server built-in:

```text
saveout <client-command>
```

captures the complete output into `command_output` with source-command metadata.

The UI also supports saving command-block output to Artifact.

Background Job output can be exported to structured JSON Artifact as well.

### Artifact source linkage

Artifact metadata can carry:

- Client/machine source
- command ID
- Job ID/name/key
- address
- timestamps
- extra metadata

Command History and Job views can therefore link back to related files instead of treating downloads as anonymous files.

---

## External Tools and `xt`

External Tools have three distinct surfaces.

### External Tool Manager UI

Metadata-driven packages/modules support:

- platform/architecture matching
- package downloads
- install
- uninstall
- install cache
- daemon instance start/stop
- one-shot execution
- runtime status
- logs
- remove stopped runtime data
- parameter presets/profiles
- preview/dry-run of the final command
- SSE lifecycle notifications

### Client runtime commands

The Client command manifest contains lower-level operations such as:

```text
external_tool_install
external_tool_uninstall
external_tool_install_status
external_tool_install_statuses
external_tool_clear_cache
external_tool_start
external_tool_run
external_tool_preview
external_tool_oneshot
external_tool_stop
external_tool_status
external_tool_list_instances
external_tool_list_instances_all
external_tool_logs
external_tool_remove
external_tool_clear_logs
external_tool_which
external_tool_exec
```

Several are intentionally hidden from normal autocomplete because the Web/Server facade is the preferred surface.

### `xt` terminal facade

The lightweight Server terminal facade operates on the selected Client and only uses package metadata whose executable is already available.

Examples:

```text
xt
xt list
xt list --json
xt info ffmpeg
xt which ffmpeg
xt ffmpeg -version
xt ffmpeg -- -i input.mp4 output.mp4
xt run ffmpeg -- -version
```

`xt install` is intentionally not supported; installation remains an External Tool Manager responsibility.

### Script SDK `xt`

Scripts can use:

```python
from client.runtime.sdk import xt

ffmpeg = xt.tool("ffmpeg")
ffmpeg.is_installed
ffmpeg.which()
result = ffmpeg("-version")

result = xt.ffmpeg("-version")
result = xt.run("ffmpeg", "-version")
```

This requires the `external_tools:catalog` Script grant and executes on the current Client.

---

## Script SDK

The current Script SDK is a real reusable surface and should not be inferred only from Script Library UI behavior.

Import:

```python
from client.runtime.sdk import artifact, command, context, keychains, workspace, xt
```

### `context`

Provides current Script/Client context:

```text
client_id
command_id
hostname
machine_id
arch
script_name
os_type
os_alias
os_ver
system_paths
script grant/token
```

### `command`

Provides reusable execution APIs:

```python
command.run_shell(...)
command.run_background(...)
command.stream_shell(...)
command.run_client(...)
command.<client_command>(...)
```

### `artifact`

Provides:

```python
artifact.save(...)
artifact.list(...)
artifact.find_by_file_name(...)
artifact.get(...)
artifact.download(...)
```

### `keychains`

Provides:

```python
keychains.create_secret(...)
keychains.create_login(...)
keychains.get_secret(...)
keychains.get_login(...)
keychains.get_keychain(...)
```

Supports machine and shared scopes.

### `xt`

Provides External Tool execution for the current Client.

### `workspace`

Provides:

- system paths from Client context
- machine-scoped pinned paths from Server
- root/home/desktop/documents/downloads/temp/executable/iCloud helpers

### Temporary Script grants

Scripts can declare API grants in:

```python
SCRIPT_METADATA["api_grants"]
```

Current grant policy includes controlled access to:

- Artifact list/download/raw/preview
- Keychain resolve/create
- External Tool catalog
- Workspace/pinned paths

Temporary grants support scope restrictions, TTL, max-use count, and Client/command binding.

---


## Jobs: current Web/runtime feature set

Background Jobs are metadata-driven and Server-managed at the catalog/UI level.

Current features include:

- Job catalog from `server/resources/jobs/`
- `JOB_METADATA`
- platform filtering
- parameter definitions
- create/upload/delete Job modules
- view Job source
- configure start parameters
- remote file/folder picker parameter types
- start / stop / stop-all
- running-job list
- Job detail dialog
- Job message dialog
- start/finish/error SSE
- Client-to-Server Job status/message reporting
- HTTP file upload for Job-produced files
- related Artifact registration
- structured Job output export to `command_output` Artifact
- Client disconnect cleanup/stop behavior

The current Client Job runtime is still thread/in-process based; process/detached Job execution remains future work.

## Script Library: current feature set

The Script Library is also metadata-driven.

Current features include:

- directory tree under `server/resources/scripts/`
- folder navigation
- remember/open the previous Script location
- create/upload/edit/rename/delete Script
- view source
- `SCRIPT_METADATA`
- platform filtering
- parameterized Run dialog
- remote file/folder picker integration
- structured `run_script` payload execution
- Script SDK context injection
- temporary Script API grant/token generation
- grant scope/TTL/max-use/client/command binding
- Artifact/Keychain/External Tool/Workspace SDK integration

`exec <script.py>` remains a lighter server built-in path, while `run_script` is the metadata/structured execution path used by the Web Script Library.


## Keychains

The project includes a dedicated Keychain Manager, not just ad-hoc credential files.

Store:

```text
runtime/keychains/
```

Scopes:

```text
machine
shared
```

Kinds:

```text
login
secret
```

A login can store:

```text
name
username
password/secret
site
note
```

A secret can store:

```text
name
secret value
note
```

List APIs hide the secret value by default and return a placeholder until the specific credential is resolved/revealed.

The Frontend includes:

- Keychain Manager
- machine/shared filtering
- create/edit/delete
- secret reveal workflow
- Password Generator dialog

The Script SDK can consume the same store through grant-controlled APIs.

---

## Clipboard

The Clipboard module supports more than text.

Current types/flows include:

- text
- image
- files
- multiple files
- directories
- directory tree metadata

The unified Clipboard dialog has Get/Send flows.

### Get

- refreshes remote Clipboard metadata
- does not automatically download every remote file to Server
- files are downloaded when explicitly requested
- directories can be downloaded as ZIP

### Send

- text
- image
- files/folders
- drag/drop file/folder selection

The target adapter handles Windows/macOS platform-specific Clipboard behavior.

Screen View also integrates Clipboard exchange/control.

---

## Screen View

Screen View is implemented as its own session protocol rather than as a foreground command.

Current behavior includes:

- open/close/error lifecycle
- frame streaming
- mouse input
- keyboard input
- dedicated Server session state
- Client disconnect cleanup
- Clipboard interaction
- SSE status/notifications

---

## PTY

PTY is also a dedicated session feature rather than a normal one-shot shell command.

- xterm.js Frontend
- Unix PTY backend
- Windows pywinpty backend
- open/close/error messages
- independent session IDs
- Client disconnect cleanup
- Server-side session release after close/error

This is separate from `shell` / `read`, which still use subprocess-style execution.

---

## Device Monitor / realtime Dashboard

The realtime Dashboard and Process views use a dedicated monitor session/channel protocol and do not occupy the normal foreground command slot.

Current channels:

```text
system
storage
network
battery
processes
apps
process_detail
process_connections
process_open_files
```

Default sampling intervals are channel-specific and can be updated while a monitor session is open.

The system/dashboard side can expose:

- CPU
- memory
- swap
- uptime
- mounted/user-visible storage volumes
- network rate/counters
- battery

Process monitoring exposes:

- process list
- app list
- process detail
- live CPU/memory detail
- network connections
- open files

Each browser tab gets its own targeted monitor SSE session so opening/changing one tab does not overwrite another tab's monitor state.

---

## Notification system

The project now has both transient notifications and a persistent Notification Center.

### Notification preferences

Current event preference keys include:

```text
connection_online
connection_offline
artifact_created
file_transfer_started
file_transfer_stopped
file_transfer_error
pty_opened
pty_closed
pty_error
screen_view_starting
screen_view_closed
screen_view_error
background_job_running
background_job_stopped
background_job_error
external_tool_daemon_started
external_tool_daemon_stopped
external_tool_daemon_error
external_tool_install_completed
external_tool_install_failed
external_tool_uninstall_completed
external_tool_uninstall_failed
agent_build_completed
agent_build_error
```

Preferences are persisted at:

```text
runtime/notification_preferences.json
```

### Notification Center

Displayed SSE notifications can be retained in:

```text
runtime/notification_center.json
```

Features:

- right-side drawer
- notification timestamp
- success/warning/error/info type
- action links
- delete one
- clear all
- deduplication by SSE `event_id` so multiple browser tabs do not create duplicate history entries

The store intentionally saves notifications that passed the Frontend preference gate and were actually shown.

---

## Toolbar Preferences and One-liners

### Toolbar Preferences

The toolbar action catalog currently contains:

```text
Remote Files
Artifacts
Info
Scripts
History
PTY
Screen View
Clipboard
External Tools
Jobs
Agents
Processes
Keychains
One-liners
```

The user can move these built-in actions between:

```text
toolbar
more
```

and persist the layout in:

```text
runtime/toolbar_preferences.json
```

### One-liners

The One-liners dialog is a small bootstrap-command catalog.

Current built-ins include:

- Python bootstrap one-liner
- Windows PowerShell bootstrap one-liner

Templates can substitute:

```text
{{server_host}}
{{server_port}}
{{web_port}}
{{file_transfer_port}}
```

The displayed snippet can be edited/copied without changing the static catalog source.

---

## Agent build, Bootstrap, and outputs

The Agent workflow includes more than the four builder modes.

Current supporting features include:

- Agent Builder dialog
- Agent Outputs dialog
- output metadata
- build status
- target platform/arch
- downloadable build artifacts
- build complete/error SSE
- bootstrap Python script
- bootstrap PowerShell script
- build/version injection
- `client_revision` injection for bundle clients
- temporary bootstrap-file cleanup

The Server does not need to continuously rebuild bundles just to detect Client source drift; revision checking is separate from artifact construction.

---

## Runtime data and persistent stores

Current Server runtime state includes at least:

```text
runtime/recent_devices.json
runtime/device_groups.json
runtime/toolbar_preferences.json
runtime/notification_preferences.json
runtime/notification_center.json

runtime/command_history/
runtime/command_history/pinned_commands/
runtime/connection_history/
runtime/pinned_paths/
runtime/keychains/
runtime/external_tool_param_presets/
runtime/web_files/
```

Important ownership:

| Runtime store | Key/scope |
| --- | --- |
| Recent Devices | machine/client history and machine ordering |
| Device Groups | `machine_id` |
| Command History | machine/connection execution history |
| Pinned Commands | `machine_id` |
| Pinned Paths | `machine_id` |
| Keychains | machine scope or shared scope |
| Toolbar Preferences | Server/user UI runtime |
| Notification Preferences | Server/user UI runtime |
| Notification Center | persisted displayed notifications |
| External Tool presets | External Tool metadata/profile context |
| Web Files / Artifacts | file results, previews, shared files, command output |

The Client has its own external runtime-config JSON outside bundled source as documented earlier in this supplement.

---


## Authentication and temporary grants addendum

In addition to the session login and static admin API token documented above, Script execution has a temporary grant mechanism.

A temporary Script grant can constrain:

- allowed API scopes
- TTL / expiration
- maximum uses
- target Client
- source command

This lets a Script call selected Server APIs without handing the Script the unrestricted admin token.

The current grant model is URL/scope-oriented rather than a full resource-level RBAC system.


## Web UI modules currently present

The current Vue UI has dedicated surfaces for:

- Device Sidebar
- Device Group Manager
- Machine Connection History
- Terminal Output / Command Blocks
- Terminal JSON viewer
- Command Input/autocomplete
- Command History
- Command Execution Detail
- Remote Files
- Remote Pins
- Remote ZIP Peek
- Remote File Picker
- Drag Upload
- Artifact Center
- Preview / Image Info
- Background Jobs / Job Detail / Job Message / Job Start
- Script Library / Script Run
- PTY
- Screen View
- Clipboard
- External Tool Manager
- Agent Builder / Agent Outputs
- Process/Apps/Process Detail
- Keychain Manager
- Password Generator
- One-liners
- Settings
- Notification Center
- Transfer Center
- Connection Info / Runtime Configuration

This list is useful when deciding whether a requested feature already has an existing UI home before creating another dialog/manager.

---


## Architecture addendum: current Frontend and Server facade

The historical README above still describes the older `static/` UI layout. The current active Frontend source is:

```text
frontend/src/
```

and uses:

```text
Vite
Vue 3
Element Plus
```

The Server application layer is assembled through `server/application/assembly.py` and exposed through a root application facade.

The root facade exposes domain sub-facades/APIs such as:

- connections
- artifacts
- command execution/history
- jobs
- scripts
- transfers
- monitor
- Clipboard
- Screen View
- External Tools
- Keychains
- pinned paths
- preferences
- Agent build/output

The newer direction deliberately avoids re-adding broad compatibility forwarding methods on the root facade; callers are expected to use the relevant domain/sub-facade.

For Web operations, the project also moved away from maintaining separate “special Server-local” UI variants for every module. Normal operational flows are Client-targeted, and a local machine can be represented by running a Client on that machine when local control is needed.


## Current Roadmap status addendum

The historical Roadmap above is intentionally left untouched, but several old checklist lines no longer represent the current code state.

### Old Roadmap items that are already implemented

The current source already includes:

- Vite + Vue 3 Frontend
- xterm.js + PTY
- Client command variables
- shell-style redirection through normal shell execution
- terminal JSON/UI toggle
- machine identity
- hide/remove device controls
- file search
- Screen View
- External Tool Manager
- realtime Dashboard
- Process monitor channels
- Notification Center
- notification preferences
- Device Groups
- Remote ZIP Peek
- Extract Here
- browser/file transfer progress
- Job output export to Artifact
- Runtime Configuration UI
- machine connection history
- Client source revision/update detection

### Existing but partial architecture work

These should be treated as **partial**, not as absent:

- **Runtime cleanup/retention**
  - preview cache startup cleanup exists
  - upload/ZIP/bootstrap operational cleanup exists
  - PTY session cleanup exists
  - no unified retention policy/service exists

- **Global broadcast vs current console**
  - SSE already supports targeted `target_tab_id`
  - empty target can broadcast
  - a formal event-scope/presentation policy is still missing

- **Capability Matrix**
  - command manifest, variable manifest, OS/arch, transfer mode, Python mode, Clipboard capabilities and other ingredients already exist
  - no single normalized capability matrix is exposed/consumed yet

- **Process migration**
  - realtime monitor channels are the modern UI path
  - older process foreground/API paths still exist in parts of the code

### Still-planned items

Current genuine future work includes:

- consistent Result/Error envelope
- removing remaining Artifact-ID-from-human-text parsing
- tests for core state machines
- History/WebTask/Job lifecycle model cleanup
- WebTask/Job persistence
- unified retention/cleanup policy
- formal event taxonomy and tab/global delivery scope
- Rules/Alerts engine
- Clipboard “Download All” with automatic archive
- Quick Actions backed by alias/custom command
- Remote PDF preview
- later Word/Excel preview
- general file tree
- multi-device batch execution
- normalized Capability Matrix
- watchdog-specific lifecycle SSE
- result `actions[]` in a structured result model
- Job process/subprocess execution
- detached Jobs
- Listener management
- operation/audit event log
- browser/system desktop notifications

### Removed/invalid Roadmap fragments

The following should not be interpreted as active features just because they appeared in old scratch TODO text:

- Sidebar drag-to-reorder was implemented experimentally and then explicitly rolled back.
- `const actionMap = {` was only an incomplete scratch fragment, not a defined feature.
- Removing Legacy HTTP transfer is no longer a current goal; Legacy remains a deliberate compatibility mode with a 1 GB upload guard.

---

## Documentation maintenance note

For future README updates, distinguish four layers:

1. **Product/module features**
   - Remote Files, Artifact, Monitor, Clipboard, External Tools, etc.

2. **Actual command entrypoints**
   - `shell`, `read`, `spawn`, `httpctl`, `gopin`, `saveout`, `xt`, etc.

3. **Reusable Script SDK**
   - `context`, `command`, `artifact`, `keychains`, `workspace`, `xt`.

4. **Runtime/persistence models**
   - machine-scoped pins/history/groups/keychains, notification/toolbar preferences, Client runtime config.

A feature should not be considered documented just because a nearby subsystem name appears once in the architecture section.
